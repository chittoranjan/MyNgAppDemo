


export class Utility {

    static hasNoError(data: any): boolean {
        if ( data == null || data === '' || data === undefined ) { return false; }

        // Get HTTP.sub(c=>{ this.})

        return true;
      }


      // Meth1


      // Met2

}
